PRÁCTICA 1: VALOR MINIMO DE RED TOROIDE con complejidad < sqrt(n)

	for i=1 to L-1
		enviaSur(mi_numero)
		recibeNorte(su_numero)
		miNumero = min(miNumero,suNumero)

	for i=1 to L-1
		enviaDerecha(mi_numero)
		recibeIzquierda(su_numero)
		miNumero = min(mi_numero,su_numero)

PRÁCTICA 2: VALOR MÁXIMO DE RED HIPERCUBO con complejidad <

	for i=1 to D
		envia(mi_numero, Vi) //Vi = Dimensión Vi-ésima
		recibe(su_numero, Vi)
		mi_numero = max(mi_numero,su_numero)
